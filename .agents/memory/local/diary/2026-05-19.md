# 2026-05-19

## `.agents/` убран из .gitignore

- Проверил содержимое `.agents/memory/local/` на чувствительные данные — чисто (плейсхолдеры, грабли PowerShell, рецепты, без паролей/IP/имён хостов).
- Убрал строку `.agents/` из `.gitignore`. Теперь папка под git.

## gitleaks подключён глобально

- Поставил `gitleaks 8.30.1` через scoop.
- Глобальный хук: `C:\Users\User\.git-hooks\pre-commit` (вызывает `gitleaks git --staged --redact --no-banner --verbose`).
- `git config --global core.hooksPath "C:/Users/User/.git-hooks"`.
- Протестировано на временном репо: 4 фейковых секрета задетектены, commit заблокирован.
- В этом репо был шим pre-commit-framework в `.git/hooks/pre-commit` без конфига (skip-on-missing-config) — теперь он не вызывается, но потерь нет (он и так ничего не делал).
- Детали и грабли — в auto-memory (`reference_gitleaks_global.md`), сюда не дублирую (общесистемное знание, не специфично для infra-init).
